node_modules
# NOTE: out/ (the built app) IS committed so `npx github:...` can run without a
# build step — npx installs prod deps only and doesn't run the prepare build.
# Rebuild + recommit it after changing src: `npm run build`.
dist
dist-electron
release
*.log
.DS_Store
.vite
# Bundled Node binary for packaging (~100MB, produced by scripts/bundle-node.mjs)
build/node
